{
  "name": "shuffle",
  "description": "Enable/Disable shuffle the playlist",
  "options": [
    {
      "name": "state",
      "description": "Enable or disable",
      "type": 5,
      "required": true
    }
  ]
}